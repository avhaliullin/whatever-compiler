struct Point(x: Int, y: Int)

struct Parallelogram(bottomLeft: Point, width: Int, height: Int)

impl Parallelogram {
    fn topRight() : Point = {
        new Point(this.bottomLeft.x + this.width, this.bottomLeft.y + this.height)
    }
}
