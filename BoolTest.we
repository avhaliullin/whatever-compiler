fn main(args: Array[String]): Unit = {
    var t: Boolean
    var f: Boolean

    t = true
    f = false

    echo(f & t)

    echo(f | t)

    echo(f ^ t)

    echo (f && t)

    echo (f || t)

    echo(!f)
}